spring-loaded-illegal-ex
========================

This project reproduces IllegalStateException on Spring Loaded 1.2.0.RELEASE

Instruction
-----------

```sh
$ git clone git@github.com:nakamura-to/spring-loaded-illegal-ex.git
$ cd spring-loaded-illegal-ex/
$ ./gradlew bootRun
```
